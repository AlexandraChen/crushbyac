<div id="happy-customers">
  <div class="container">
    <div class="row">
    <p id="panel-title"> <%= t("happy_customers") %> </p>
      <% if Client.all == nil %>
        <p> No clients pics </p>
      <% else %>
        <% Client.all.each do |client| %>
          <ul class="client-list">
            <li><%= image_tag client.img.url(:medium), class: "client-img"%>
            </li>%=  %>
        <% end %>
        </ul>
      <% end %>
    </div>
    <% if current_page?("/") %>
      <p id="clients-gallery"><%= link_to "Ver galería completa", happy_customers_path %></p>
    <% end %>
  </div> 
</div>