<!DOCTYPE html>
<html>
<head>
  <title>CRUSH by AC</title>
  <link rel="shortcut icon" href="/assets/sites/icon_header.jpg">
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= javascript_include_tag 'application'%>
  <script src="https://maps.googleapis.com/maps/api/js"></script>
  <script>
    function initialize() {
      var mapProp = {
        center:new google.maps.LatLng(8.9844594, -79.5295954),
        zoom:17,
        mapTypeId:google.maps.MapTypeId.ROADMAP
      };
      var map=new google.maps.Map(document.getElementById("googleMap"),mapProp);
      var marker = new google.maps.Marker({
        position: mapProp.center,
        map: map,
        title: 'Crush location'
      });
    }
    google.maps.event.addDomListener(window, 'load', initialize);
  </script>
</head>
<body>
  <%= render partial: 'sites/navbar' %>
  <% if notice %>
    <div id="notice"><p><%= notice %></p></div>
  <% elsif alert %>
    <div id="alert"><p><%= alert %></p></div>
  <% end %>
  
  <div class="wrap">
    <%= yield %>
    <a href="#" class="scrollup">Scroll</a>
  </div>
  <%= render partial: 'sites/footer' %>

  <script>
    var router = new Router()
  </script>

</body>
</html>

