<div class="container spacer">
  <div class="row">
    <h1> Admin panel </h1>
    <p><%= link_to t("log_out"), '/logout' %></p>
    <p><%= link_to t("add_product"), new_product_path %></p>
    <p><%= link_to t("add_client"), new_client_path %></p>

    <div class="col-sm-4">
    <h4> Published <h4>
      <% @products.each do |product| %>
        <% if product.published == true %>
          <li><%= image_tag product.img.url(:thumb)%>
            ID <%= product.id %> | <%= link_to product.title, product_path(product.id) %> | <%= link_to t("edit_product"), edit_product_path(product.id) %>
          </li> 
        <% end %>
      <% end %>
    </div>
    <div class="col-sm-4">
    <h4> Unpublished <h4>
      <% @products.each do |product| %>
        <% if product.published != true %>
          <li><%= image_tag product.img.url(:thumb)%>
            ID <%= product.id %> | <%= link_to product.title, product_path(product.id) %> | <%= link_to t("edit_product"), edit_product_path(product.id) %>
          </li> 
        <% end %>
      <% end %>
    </div>
    <div class="col-sm-4">
    <h4> Clients <h4>
      <% @clients.each do |client| %>
          <li><%= image_tag client.img.url(:thumb)%>
            ID <%= client.id %> 
          </li> 
      <% end %>
    </div>
  </div>
</div>